import { Routes } from '@angular/router';

/// The routes array is empty
/// This is because the application does not have any routes
/// The application is a standalone application
export const routes: Routes = [];
